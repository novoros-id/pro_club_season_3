# Project overview

- Serena project: `goalkeeper`; package name in `pubspec.yaml`: `goalkeeper_trainer`; description: goalkeeper training app.
- Entry point: `lib/main.dart`; app root: `GoalkeeperApp`.
- Main source areas: `lib/core`, `lib/features`, `lib/l10n`, `assets`.
- Existing feature areas under `lib/features`: registration, home, settings, diary, analytics, authors, game1, game2, game_schulte.
- Current code search found no `DailyTask` or `DailyTaskCompletion` symbols; daily tasks are not implemented yet.
- Sources checked via Serena: `lib/main.dart`, `lib/features/**`, `pubspec.yaml`.